Setting up the PI

1) copy asound.conf to /etc/
2) add full path to startup script followed by '&' to /etc/rc.local
3) chmod 755 rc.local AND the startup script
4) copy contents of slashbootslashconfig.txt to /boot/config.txt
